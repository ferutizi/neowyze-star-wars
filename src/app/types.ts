export type FilmType = {
  title: string,
  episode_id: number,
  characters: string[],
  created: string,
  director: string,
  edited: string,
  opening_crawl: string,
  planets: string[],
  producer: string,
  release_date: string,
  species: string[],
  starships: string[],
  url: string,
  vehicles: string[]
}